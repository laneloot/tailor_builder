{
  "name": "free_tailor",
  "private": true,
  "scripts": {
    "dev": "sh -c 'trap \"kill 0\" EXIT INT TERM; npm run dev --prefix backend & npm run dev --prefix frontend & wait'",
    "dev:live": "sh -c 'trap \"kill 0\" EXIT INT TERM; npm run dev --prefix backend & npm run dev:live --prefix frontend & wait'",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:frontend-live": "npm run dev:live --prefix frontend",
    "test": "npm run test --prefix backend",
    "test:backend": "npm run test --prefix backend"
  }
}
